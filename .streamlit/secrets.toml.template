# Streamlit Cloud Secrets
# Go to: your app → Settings → Secrets → paste this in
#
# ⚠ Replace the value with your actual key. Never commit this file.

ANTHROPIC_API_KEY = "sk-ant-api03-..."

# Optional: CockroachDB connection string (if using DB instead of CSV)
# DATABASE_URL = "postgresql://..."
